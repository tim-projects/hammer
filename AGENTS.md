# Agent Directives

Add high-level tasks here. Your AI agent will monitor this file, create formal records in the `tasks` system, and execute the work autonomously.

## New Tasks
- [ ] Fix the CSS alignment on the primary navigation bar.
- [ ] Investigate why the SQLite database is locking during heavy writes.
- [ ] Add a `--version` flag to the CLI.

## Instructions
1. Add a directive above.
2. Run your AI agent.
3. The agent will move items to `tasks/backlog` and begin execution.
