diff --git a/diff_debug.log b/diff_debug.log
index 7845b08..8a247b7 100644
--- a/diff_debug.log
+++ b/diff_debug.log
@@ -1 +1,11 @@
 branch=143-issue-bug-repo-sync-loops-on-up-to-d, main_sha=196e3d376385f68b4ec33b18927df92846e49db0, diff_len=0
+<<<<<<< HEAD
+<<<<<<< HEAD
+branch=143-issue-bug-repo-sync-loops-on-up-to-d, main_sha=e39271c3bf595c717dd0d80d44b3ddd6669d20b5, diff_len=0
+=======
+branch=149-task-audit-test, main_sha=e39271c3bf595c717dd0d80d44b3ddd6669d20b5, diff_len=0
+>>>>>>> 150-task-harden-regression-audit-with-c
+=======
+branch=149-task-audit-test, main_sha=e39271c3bf595c717dd0d80d44b3ddd6669d20b5, diff_len=0
+>>>>>>> 155-task-implement-30-day-cleanup-grace
+branch=155-task-implement-30-day-cleanup-grace, main_sha=e840f18bb4c3c8423b6be3b1b69ec7c0bf269641, diff_len=5885
diff --git a/repo.py b/repo.py
index 96186f4..9e43b4a 100644
--- a/repo.py
+++ b/repo.py
@@ -260,9 +260,13 @@ def cmd_merge(src_input, target_input, auto_commit=True, force_pipeline=False):
         run(["git", "pull", PRIMARY_REMOTE, target], check=False)
     else:
         warn("No remote - skipping pull")
+    # Set flag for Git hook bypass
+    env = os.environ.copy()
+    env["HAMMER_INTERNAL_MERGE"] = "1"
     run(
         ["git", "merge", src, "-m", f"merge: {src} into {target}"],
         context=f"merge {src}→{target}",
+        env=env,
     )
     if check_remote_exists():
         st = run(["git", "status", "--porcelain"], capture=True).stdout.strip()
diff --git a/tasks.py b/tasks.py
index 80fc41c..f9953fd 100755
--- a/tasks.py
+++ b/tasks.py
@@ -114,11 +114,9 @@ if __name__ == "__main__":
     cr_p.add_argument(
         "--repro", nargs="+", help="Reproduction steps for issues (list)."
     )
-    cr_p.add_argument(
-        "--branch",
-        action="store_true",
-        help="(ignored) Branch name is auto-generated from title.",
-    )
+    cr_p.add_argument("--branch", action="store_true", help="Auto-generate branch.")
+
+    aud_p = subparsers.add_parser("audit", help="Generate an audit log after reviewing the patch.")
 
     mod_p = subparsers.add_parser("modify", help="Update task.")
     mod_p.add_argument("filename", help="Task Id (or filename).")
@@ -282,6 +280,8 @@ if __name__ == "__main__":
             cli.list(show_all=False)
     elif args.command == "checkpoint":
         cli.checkpoint(args.filename)
+    elif args.command == "audit":
+        cli.audit(args.id)
     elif args.command == "link":
         cli.link(args.filename, args.blocked_by)
     elif args.command == "reconcile":
@@ -296,5 +296,6 @@ if __name__ == "__main__":
         cli.run_tool(args.tool, fix=args.fix)
     elif args.command == "undo":
         cli.undo(args.filename)
+        cli.undo(args.filename)
     elif args.command == "doctor":
         cli.doctor(fix=args.fix)
diff --git a/tasks_ai/cli.py b/tasks_ai/cli.py
index 48b3d40..a2f21d7 100644
--- a/tasks_ai/cli.py
+++ b/tasks_ai/cli.py
@@ -540,6 +540,19 @@ class TasksCLI:
         return False
 
     def init(self, force=False):
+        def install_hooks(self):
+            hook_dir = os.path.join(self.root, ".git", "hooks")
+            hook_path = os.path.join(hook_dir, "pre-merge")
+            
+            # Ensure hook directory exists
+            if not os.path.exists(hook_dir):
+                return
+
+            # Explicitly overwrite ONLY the pre-merge hook
+            with open(hook_path, "w") as f:
+                f.write("#!/bin/bash\n\nif [ \"$HAMMER_INTERNAL_MERGE\" == \"1\" ]; then\n    exit 0\nfi\ntarget_branch=$(git rev-parse --abbrev-ref HEAD)\nif [ \"$target_branch\" == \"main\" ]; then\n    echo \"⚠️  Direct git merge to main detected. Pipeline governance requires './hammer repo merge'. Aborting.\"\n    exit 1\nfi")
+            os.chmod(hook_path, 0o755)
+            self.log("Git pipeline enforcement hook (pre-merge) installed/updated.")
         if self.dev:
             for folder in list(STATE_FOLDERS.values()):
                 p = os.path.join(self.tasks_path, folder)
@@ -840,6 +853,20 @@ class TasksCLI:
         )
         return current
 
+    def audit(self, task_id):
+        """Generate an audit log after reviewing the patch."""
+        filepath, _ = self.find_task(task_id)
+        if not filepath:
+            self.error(f"Task {task_id} not found.")
+        
+        # Use filename as task_id (e.g., 152-task-harden-audit-test)
+        task_id = os.path.basename(filepath).rsplit(".", 1)[0]
+        audit_path = f".tasks/review/{task_id}.audit"
+        with open(audit_path, "w") as f:
+            f.write(f"Audited by: {os.getlogin()}\nTime: {os.times()}\n")
+            
+        self.log(f"✅ Audit log created for task {task_id} at {audit_path}")
+
     def create(
         self,
         title,
@@ -1087,6 +1114,13 @@ class TasksCLI:
 
         if regression_check is not None:
             if regression_check:
+                # Check for audit log
+                audit_path = f".tasks/review/{task_id}.audit"
+                if not os.path.exists(audit_path):
+                    self.error(
+                        "Cannot set regression check without audit log.",
+                        hint=f"Review the patch and run 'hammer tasks audit {task_id}' first."
+                    )
                 task.metadata["Rc"] = True
             else:
                 task.metadata["Rc"] = ""
@@ -1992,11 +2026,10 @@ class TasksCLI:
         ]:
             task = FM.load(filepath_str)
             if not task.metadata.get("Rc"):
-                patch_path = f".tasks/review/{task_id}.patch"
                 self.error(
                     f"Cannot move to {new_status}: regression check not passed (Rc flag not set).",
                     hint=f"Complete the regression check before promoting.\n"
-                    f"  1. Review the diff patch at {patch_path}\n"
+                    f"  1. Review the diff patch at .tasks/review/{task_id}.patch\n"
                     "  2. Audit for regressions and side-effects\n"
                     f"  3. Run: ./hammer tasks modify {task_id} --regression-check",
                 )
diff --git a/tasks_ai/constants.py b/tasks_ai/constants.py
index a7d6d88..623cd8d 100644
--- a/tasks_ai/constants.py
+++ b/tasks_ai/constants.py
@@ -92,3 +92,4 @@ AGENT OPERATIONAL PROTOCOL:
  12. ERROR RECOVERY: If a command fails, read the 'error' field in the JSON response. 
      The 'hint' provides actionable next steps.
 """
+RETENTION_DAYS = 90
diff --git a/tasks_ai/cli.py b/tasks_ai/cli.py
index a2f21d7..cf69e04 100644
--- a/tasks_ai/cli.py
+++ b/tasks_ai/cli.py
@@ -2435,6 +2435,12 @@ class TasksCLI:
             self.finish()
 
     def reconcile(self, target=None, all=False):
+        # Load retention days from config if exists, default 90
+        # For now, hardcode check to constants.RETENTION_DAYS
+        
+        # Logic: check if branch merged
+        # If merged, move to DONE
+        pass
         if not target and not all:
             self.cleanup(dry_run=True)
         elif all:
